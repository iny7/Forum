import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  btn: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  label: {
    marginLeft: 4
  }
})

export default styles
